# 4) Escreva um algoritmo para ler as dimensões de um retângulo (base e altura), calcular e escrever a 
# área do retângulo.

#o calculo de de um retangulo é a base vezes a altura então  a = b * h

b = 24
h = 30

area = b * h

print(f"a area do retangulo e: {area}")
